# Boost Collections

Imported 61 legacy-to-current collection-name mappings from `AUDIOS - old and new bundles_collections.xlsx`. Legacy and display names are both preserved.

The source contains no Boost-to-collection membership table. Collection membership must therefore remain empty until supplied or verified by Terri.
